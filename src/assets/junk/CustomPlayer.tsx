import React, { useState, useRef } from "react";
import ReactPlayer from "react-player";

type CustomerControlsT = {
  StoryPoster: string;
  MediaUrl: string;
};

const CustomControls: React.FC<CustomerControlsT> = ({
  StoryPoster,
  MediaUrl,
}) => {
  

  return (
    <div>
      
    </div>
  );
};

export default CustomControls;
